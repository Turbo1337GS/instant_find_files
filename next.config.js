/** @type {import('next').NextConfig} */
const nextConfig = {
    assetPrefix: "/IFF",
    publicRuntimeConfig: {
        basePath: "/IFF",
    },
    experimental: {
        basePath: "/IFF",
    },
    basePath: '/IFF',
}

module.exports = nextConfig
